# 🚀 项目启动指南 (Startup Guide)

本项目包含 **Python 后端** (FastAPI) 和 **Vue 前端** (Vue 3) 两部分。
请打开两个终端窗口，分别启动它们。

---

## 🟢 1. 启动后端 (Backend)

后端负责大模型交互、算法计算和数据存储。

### 步骤
1.  **进入目录**:
    ```powershell
    cd media/code/media-backend
    ```
2.  **激活环境** (如果未激活):
    ```powershell
    # 假设您的虚拟环境在 lab 根目录
    ..\..\..\.venv\Scripts\Activate.ps1
    ```
3.  **启动服务**:
    ```powershell
    uvicorn app.main:app --reload
    ```

### 成功标志
看到类似以下的输出即表示成功：
```text
INFO:     Uvicorn running on http://127.0.0.1:8000 (Press CTRL+C to quit)
INFO:     Application startup complete.
```

---

## 🔵 2. 启动前端 (Frontend)

前端负责可视化界面和用户交互。

### 步骤
1.  **进入目录** (新建一个终端):
    ```powershell
    cd media/code/media-frontend
    ```
2.  **安装依赖** (如果第一次运行):
    ```powershell
    npm install
    ```
3.  **启动开发服务器**:
    ```powershell
    npm run serve
    ```

### 成功标志
看到类似以下的输出：
```text
  App running at:
  - Local:   http://localhost:8080/
```

---

## 🌐 3. 访问项目

打开浏览器访问前端地址（通常是 **http://localhost:8080**）。

*   **Story Dashboard (实时创作)**: `http://localhost:8080/#/`
*   **Analysis Dashboard (数据分析)**: `http://localhost:8080/#/analysis` (查看 Human vs AI 对比图)

---

## ⚠️ 常见问题

1.  **后端报错 `ModuleNotFoundError`**:
    *   检查是否激活了正确的 `.venv`。
    *   检查是否安装了依赖: `pip install -r requirements.txt`。

2.  **前端显示 "Network Error"**:
    *   检查后端是否挂了。
    *   确保后端端口是 `8000` (前端默认代理到 8000)。
